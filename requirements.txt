# --- Server (control panel backend) ---
fastapi>=0.111
uvicorn[standard]>=0.30

# --- Agent (runs on the managed PC) ---
websockets>=12.0
psutil>=5.9
mss>=9.0
pillow>=10.0
pynput>=1.7
opencv-python>=4.9
pywin32>=306; sys_platform == "win32"
